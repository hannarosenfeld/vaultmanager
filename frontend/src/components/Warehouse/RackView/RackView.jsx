import React, { useState } from "react";import RackInfo from "./RackInfo";import PalletModal from "./PalletModal";function RackView({ selectedRack, handleAddPalletClick, handleEditPalletClick }) {  const [isModalOpen, setIsModalOpen] = useState(false);  const [selectedPallet, setSelectedPallet] = useState(null);  const handleEditPalletClickLocal = (pallet) => {    setSelectedPallet(pallet);    setIsModalOpen(true);  };  const handleModalClose = () => {    setIsModalOpen(false);    setSelectedPallet(null);  };  const handleSavePallet = (updatedData) => {    console.log("Saving pallet:", updatedData);    // Dispatch updatePalletThunk here  };  return (    <div>      <RackInfo        selectedRack={selectedRack}        handleAddPalletClick={handleAddPalletClick}        handleEditPalletClick={handleEditPalletClickLocal} // Pass the function here      />      <PalletModal        pallet={selectedPallet}        isOpen={isModalOpen}        onClose={handleModalClose}        onSave={handleSavePallet}      />    </div>  );}export default RackView;